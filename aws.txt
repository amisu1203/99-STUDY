액세스 키
AKIAYVRSBGCCETEDHSFX

비밀 액세스 키
7kvEv2qC9LA/xQ9DskHvTGdn9tXLcl+B5UEqIynU
- 터미널 준비하기 - 
mkdir deploy (deploy라는 폴더 생성)
cp app.py deploy/application.py (app.py를 복사해서 dep/app.py에 넣어라)
 cp -r templates deploy/templates (temp 복사해서 dep/temp에 넣어라)
pip freeze > deploy/requirements.txt (venv에 설치된 라이브러리 쫙 명세해라)
cd deploy

- application.py 세팅하기 - 
application = app = Flask(__name__)
app.run()

- 패키지 설치하기 - 
pip install awsebcli

- 보안 자격증명 - 
eb init

- 초기 설정 - 
eb create myweb

- 코드 수정 & 업데이트 - 
eb deploy myweb